// Enum :
// enum is the set of value which returns index or define index or return or return value

// syntax 
// enum name {}

enum Students{
    ali,yaqoob,sufiyan,asghar
}

const student : Students= Students.sufiyan;
console.log(student);

enum off_Management{
    shakeel,ali,mirza,yousuf
}
const office : off_Management = off_Management.ali;
console.log(office);